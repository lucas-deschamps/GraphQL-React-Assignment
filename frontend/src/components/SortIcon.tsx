import React from 'react'
import sortIcon from '../static/sort-icon.png';

const SortIcon = (): JSX.Element => (
  <img alt="column sort icon" src={sortIcon} className="h-3 w-3 mx-0.5" />
);

export default SortIcon;
